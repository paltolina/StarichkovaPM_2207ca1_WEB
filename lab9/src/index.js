import React from 'react';
import ReactDOM from 'react-dom';
import CitySelector from './task_validation2';

ReactDOM.render(
  <React.StrictMode>
    <CitySelector />
  </React.StrictMode>,
  document.getElementById('root')
);
